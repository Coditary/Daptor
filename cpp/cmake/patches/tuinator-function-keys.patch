--- a/include/tuinator/core/event.hpp
+++ b/include/tuinator/core/event.hpp
@@ -22,8 +22,20 @@ enum class Key : std::uint16_t {
     Home,
     End,
     PageUp,
     PageDown,
+    F1,
+    F2,
+    F3,
+    F4,
+    F5,
+    F6,
+    F7,
+    F8,
+    F9,
+    F10,
+    F11,
+    F12,
     Resize,
     Unknown,
 };

--- a/src/backend/curses_backend.cpp
+++ b/src/backend/curses_backend.cpp
@@ -45,6 +45,14 @@ const char* mouse_debug_log_path() {
 #endif
 }
 
+Key function_key_from_code(int ch) {
+    return static_cast<Key>(static_cast<int>(Key::F1) + (ch - KEY_F(1)));
+}
+
+bool is_function_key(int ch) {
+    return ch >= KEY_F(1) && ch <= KEY_F(12);
+}
+
 Key map_key(int ch) {
     switch (ch) {
     case KEY_RESIZE: return Key::Resize;
@@ -698,6 +706,10 @@ std::optional<Event> CursesBackend::read_event(bool block) {
         return ctrl_press;
     }
 
+    if (is_function_key(ch)) {
+        return KeyPress{function_key_from_code(ch), '\0'};
+    }
+
     KeyPress press{};
     press.key = Key::Unknown;
     if (ch >= 32 && ch <= 126) {
